# =============================================================================
# ThetaMind 环境变量配置模板
# =============================================================================
# 
# 使用方法：
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 填写所有必需配置（标记为 [必需]）
# 3. 根据需要填写可选配置
# 4. 不要提交 .env 文件到 Git（已加入 .gitignore）
#
# 配置优先级（从高到低）：
# 1. Docker environment: 中的设置（仅用于容器内部配置，用户不应修改）
# 2. 系统环境变量（export VAR=value）
# 3. .env 文件（本文件）
# 4. config.py 中的默认值
# =============================================================================

# =============================================================================
# [必需] 数据库配置
# =============================================================================
DB_USER=thetamind
DB_PASSWORD=your_secure_password_here
DB_NAME=thetamind
DB_PORT=5432

# Database URL (会自动根据上面的配置构建，但也可以直接设置)
# DATABASE_URL=postgresql+asyncpg://thetamind:password@db:5432/thetamind

# =============================================================================
# [必需] Google OAuth 配置（用户登录必需）
# =============================================================================
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Google API Key [必需] 当 AI_PROVIDER=gemini 时必须配置
GOOGLE_API_KEY=your_google_api_key_here  # [必需] 当使用 gemini provider 时需要

# =============================================================================
# [必需] JWT 认证配置
# =============================================================================
JWT_SECRET_KEY=your_super_secret_jwt_key_change_this_in_production

# =============================================================================
# AI Provider 配置
# =============================================================================
# 选择 AI 提供商: gemini (默认, 直接API) 或 zenmux
AI_PROVIDER=gemini

# ZenMux 配置（当 AI_PROVIDER=zenmux 时需要）
ZENMUX_API_KEY=your_zenmux_api_key_here
ZENMUX_MODEL=gemini-3.0-pro-preview
ZENMUX_API_BASE=https://api.zenmux.com

# AI 模型配置（可选，使用默认值即可）
# AI_MODEL_TIMEOUT=90
# AI_MODEL_DEFAULT=gemini-3.0-pro-preview  # 所有用户使用的标准模型（Free # AI_MODEL_DEFAULT=gemini-3-pro-preview  # 仅在使用 gemini provider 时有效 Pro）
# AI_MODEL_FALLBACK=deepseek-chat

# =============================================================================
# Tiger Brokers API 配置（可选）
# =============================================================================
# TIGER_API_KEY=your_tiger_api_key
# TIGER_API_SECRET=your_tiger_api_secret
# TIGER_ID=your_tiger_id
# TIGER_ACCOUNT=your_tiger_account
# TIGER_PRIVATE_KEY=your_tiger_private_key_or_path
# TIGER_SANDBOX=true
# TIGER_PROPS_PATH=/path/to/tiger_openapi_config.properties

# =============================================================================
# Lemon Squeezy 支付配置（可选，仅在生产环境需要）
# =============================================================================
# LEMON_SQUEEZY_API_KEY=your_lemon_squeezy_api_key
# LEMON_SQUEEZY_WEBHOOK_SECRET=your_webhook_secret
# LEMON_SQUEEZY_STORE_ID=your_store_id
# LEMON_SQUEEZY_VARIANT_ID=your_variant_id
# LEMON_SQUEEZY_VARIANT_ID_YEARLY=your_yearly_variant_id

# =============================================================================
# 应用环境配置
# =============================================================================
ENVIRONMENT=development
# DEBUG=false

# =============================================================================
# Redis 配置（可选，使用默认值即可）
# =============================================================================
REDIS_URL=redis://redis:6379/0
REDIS_PORT=6379

# =============================================================================
# 时区配置（可选）
# =============================================================================
# TIMEZONE=UTC

# =============================================================================
# 前端配置
# =============================================================================
FRONTEND_PORT=3000
VITE_API_URL=http://localhost:5300
# VITE_GOOGLE_CLIENT_ID 会从 GOOGLE_CLIENT_ID 自动传递

# =============================================================================
# 后端配置
# =============================================================================
BACKEND_PORT=5300

# =============================================================================
# Nginx 配置（可选）
# =============================================================================
NGINX_PORT=80
NGINX_SSL_PORT=443

# =============================================================================
# 生产环境配置（可选，仅在生产环境需要）
# =============================================================================
# DOMAIN=https://thetamind.com
# ALLOWED_ORIGINS=https://thetamind.com,https://www.thetamind.com

# =============================================================================
# 数据库连接池配置（可选，使用默认值即可）
# =============================================================================
# DB_POOL_SIZE=20
# DB_MAX_OVERFLOW=10

# =============================================================================
# Mock 数据模式（可选，用于测试）
# =============================================================================
# USE_MOCK_DATA=false
