# ============================================
# 🟢 基础设置 (Infrastructure)
# ============================================
# 注意：在 Docker 内部运行时，host 必须是 'db' 和 'redis' (对应 docker-compose service name)
# 如果你在本地直接运行 python main.py，请改回 'localhost'

# PostgreSQL 数据库连接
# 格式: postgresql+asyncpg://user:password@host:port/dbname
DATABASE_URL=postgresql+asyncpg://thetamind:changeme_password@db:5432/thetamind
DB_PASSWORD=your_secure_password_here

# 数据库连接池配置
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=10

# Redis 连接
# 格式: redis://host:port/db
REDIS_URL=redis://redis:6379/0

# ============================================
# 🐯 老虎证券 (Tiger Brokers)
# ============================================
# 必须配置 Open SDK (Python SDK)
TIGER_ID=2015xxxx
TIGER_ACCOUNT=138xxxx  # 你的资金账号
TIGER_SANDBOX=true     # 开发阶段设为 true，上线改 false

# 私钥配置 (二选一)
# 方案 A: 填写私钥文件在 Docker 容器内的路径 (需挂载)
# TIGER_PRIVATE_KEY=/app/certs/tiger_private_key.pem
# 方案 B (推荐): 直接填写私钥内容，将换行符替换为 \n
TIGER_PRIVATE_KEY=-----BEGIN RSA PRIVATE KEY-----\nMIIEpQIBAAKCAQEA...\n...-----END RSA PRIVATE KEY-----

# Legacy Keys (通常不需要，除非你用旧版 API)
TIGER_API_KEY=
TIGER_API_SECRET=

# ============================================
# 🧠 Google AI & Auth
# ============================================
# Gemini 模型调用 Key
GOOGLE_API_KEY=AIzaSyD...

# 前端 Google 登录组件凭证
GOOGLE_CLIENT_ID=789...apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-...

# ============================================
# 🍋 Lemon Squeezy (支付)
# ============================================
# API Key (用于创建 Checkout 链接)
LEMON_SQUEEZY_API_KEY=eyJhbGc...

# Webhook 签名密钥 (必须与 LS 后台配置一致)
LEMON_SQUEEZY_WEBHOOK_SECRET=your_custom_secret_string

# [新增] 商店与产品 ID (代码中依赖这俩变量)
LEMON_SQUEEZY_STORE_ID=12345
LEMON_SQUEEZY_VARIANT_ID=67890

# ============================================
# 🔐 安全与认证 (Security)
# ============================================
# 运行 `openssl rand -hex 32` 生成一个新的
JWT_SECRET_KEY=change_this_to_a_secure_random_string_in_production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# ============================================
# ⚙️ 应用环境 (App Config)
# ============================================
# 选项: development (允许跨域, Docs开启), production (严格安全)
ENVIRONMENT=development
DEBUG=true

# ============================================
# 🤖 AI 模型配置
# ============================================
AI_MODEL_TIMEOUT=60
AI_MODEL_DEFAULT=gemini-1.5-pro  # 目前可用模型，等 3.0 发布后改为 gemini-3.0-pro
AI_MODEL_FALLBACK=gemini-1.5-flash

# ============================================
# 🌍 时区
# ============================================
# 必须是 UTC，业务逻辑中会转换为 US/Eastern
TIMEZONE=UTC